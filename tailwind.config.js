const colors = require(`tailwindcss/colors`)
module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
   colors:{
     blue:colors.lightBlue,
   }
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
